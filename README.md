#notas
este es mi primer programa en node
```
--help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     es la base de la tabla de multiplicar                [required]
  -l, --listar   muestra en pantalla la tabla de multiplicar
                                                      [required] [default: true]
  -h, --hasta    rango de la tabla de multiplicar                     [required]

```
